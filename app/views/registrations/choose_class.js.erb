<% if @registered %>
  $("#course-<%=@course.id %>").addClass("registered");
  location.reload()
<% else %>
  location.reload()
  $("#course-<%= @course.id %>").append("<br><span class='alert'><%= @error_content %></span>");
<% end %>

$(document).trigger("load")
